# Ingestion

Crawlers, parsers and the clause-aware chunking pipeline live here, built in
Step 2 (crawlers + raw document store with provenance) and Step 3 (parsing,
chunking, embedding, indexing) of the build plan. Empty scaffold for now —
see the root `README.md` "Status" section and `docs/DECISIONS.md`.
