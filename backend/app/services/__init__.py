"""Business logic layer.

Per the architecture spec, route handlers under `app.api` must not contain
business logic — they call service classes defined here. Empty in Step 1;
populated starting Step 6 (conversation/message services) and Step 8
(certification rules engine service).
"""
