"""Manak Sahayak backend — FastAPI service.

This package hosts the API surface, orchestration layer, persistence models and
service classes described in docs/ARCHITECTURE.md. Route handlers under
`app.api` must stay thin: all business logic lives in `app.services`.
"""
