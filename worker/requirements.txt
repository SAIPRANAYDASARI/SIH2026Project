# Kept identical to backend/requirements.txt (the worker imports app.* code
# directly, see celery_app.py) plus the worker-only Celery pin duplicated
# for clarity. If you change one, change both — Step 3 introduces a shared
# constraints file once ingestion adds its own heavier deps (pdf parsing,
# embeddings) that the API process doesn't need.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
redis==5.2.1
celery==5.4.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
httpx==0.28.1
orjson==3.10.12
structlog==24.4.0
tenacity==9.0.0

# Ingestion (see ../ingestion/requirements.txt — the worker is what
# actually runs crawlers, via worker/tasks/ingestion.py)
beautifulsoup4==4.12.3
lxml==5.3.0
pypdf==5.1.0
